# ==============================================
# Production Environment Variables for Backend
# ==============================================

# Server Configuration
PORT=8080
NODE_ENV=production

# CORS Configuration
# Primary CORS origin (your main frontend domain)
CORS_ORIGIN=https://video-genai31.vercel.app

# Additional CORS origins (comma-separated)
# Include any other domains that need access
ADDITIONAL_CORS_ORIGINS=https://your-staging-domain.vercel.app,https://your-custom-domain.com

# Google GenerativeAI API
GOOGLE_API_KEY=your_google_api_key_here

# MongoDB Configuration
MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/video-gen?retryWrites=true&w=majority

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# S3 Configuration
S3_BUCKET_NAME=your-video-bucket
S3_BUCKET_REGION=us-east-1

# ==============================================
# Deployment Instructions:
#
# For Google Cloud Run:
# 1. Set these as environment variables in Cloud Run console
# 2. Or use: gcloud run deploy --set-env-vars KEY=VALUE
#
# For Railway/Render/Heroku:
# 1. Add these in the dashboard environment variables section
#
# For Docker:
# 1. Use docker run -e KEY=VALUE
# 2. Or create a .env file and use docker run --env-file .env
# ==============================================
